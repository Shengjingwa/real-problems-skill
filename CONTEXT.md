# 项目术语与领域词汇 (CONTEXT)

本仓库分发一个专门用于严格治理规则、Prompt、Skill 及操作 SOP 的 Agent 技能（Skill）。为了保证在全流程中人与 Agent 沟通术语精确无歧义，本文档定义了核心概念与标准用词。

完整的工作流执行步骤详见：`skills/real-problems/SKILL.md`。

---

## 核心术语表

### 1. 对象 (Target Document)
本次需要修改的、已经保存在磁盘上的规则或说明文件。涵盖三大类：
- **AI 提示词、技能与工作流**（Agent Skill、System Prompt、多 Agent 协作路由、Eval 评测标准）
- **系统架构与工程规范**（`AGENTS.md`、ADR 架构决策、代码审查清单、API 契约）
- **运维、安全与业务操作手册**（应急 Runbook、发布清单、SOP、带明确验收标准的需求/测试规格）

*避免使用：目标文件、待改文件、输入*

---

### 2. 用户指定改动 (User-Directed Change)
用户已经拍板、不需要论证的改动，例如改名、改 `description`、换斜杠命令、统一某个术语叫法。它不过真问题的两条杠，用户提出即成立；流程的职责是登记诉求、评估影响面、随其它改动原子执行，而不是论证它该不该改。全程与真问题分开记录、分开列示。

*避免使用：需求、用户建议、主观改动*

---

### 3. 完成条件 (Completion Criteria)
对象正文中已写明、或经人工确认获批的客观检验判据。来源有三种：原文摘录、第 1 步提炼获批、第 5 步由候选标准升格。只认这三种来源，不引入未经确认的外部标准。

*避免使用：验收标准、DoD、需求*

---

### 4. 真问题 (True Problem)
同时满足以下两个硬性条件的缺陷：
1. **能精准定位**：能在原文中指出具体的行号、语句或可观察行为。
2. **不改必定失败**：如果不修复，对象就无法达成已获批的完成条件，或与项目实际代码/环境脱节导致执行报错，或缺失关键异常/退出分支导致死锁卡死。

*避免使用：问题、改进点、优化项、issue*

---

### 5. 候选 (Candidate)
某位评审者在初步审查中提炼出的“疑似真问题”，尚未经过真问题硬性条件的最终检验。

*避免使用：发现、建议、finding*

---

### 6. 评审模式 (Review Mode)
第 2 步闸门由人二选一的分工范式，决定第 3 步怎么派活：
- **独立陪审团**：多名评审者各用不同模型，拿相同的完整输入独立跑完整轮评审，彼此不通气。重复发现是置信信号。
- **分区覆盖**：先集中调研只交事实，再把完成条件切成互不重叠的角度分头审查，保证不漏检。

*避免使用：并行策略、审查方式、分工模式*

---

### 7. 陪审员 (Juror)
独立陪审团模式下的一名评审 subagent。拿到对象全文与全部完成条件，在独立上下文里完成调研、通读、分析全过程，不得看到其它陪审员的结论。默认派 3 名，够产生多数信号，用户可改数量。

*避免使用：评审员、reviewer、审查 agent*

---

### 8. 角度 (Perspective)
分区覆盖模式下的分工单位。把完成条件与调研事实划分为 2~4 个互不重叠的角度（如项目实际可行性对照、指令完整性、异常分支可判定性、术语一致性等），每条内容恰好归一个角度。

*避免使用：维度、视角、检查项*

---

### 9. 评审阵容 (Model Roster)
派发 subagent 时的模型分派规则，适用于第 3 步的评审者与第 6 步的改法者、挑刺者。硬要求只有独立上下文；模型多样性是增强手段而非准入门槛。用户点名则严格照办，点名的模型不可用时停下报告，不得静默替换；用户没点名则从可用模型里尽量挑两两不同的；可用模型不够时照常派满并在笔记标注，同模型的重合结论不计入置信信号。

*避免使用：模型配置、model 设置、分工模型*

---

### 10. 闸门 (Human Gate)
流程中停下来等待人工做决定的确认环节，共三道：调研与评审模式、清单、最终方案。用户未做选择，流程绝对不往下执行。

*避免使用：确认、审批、checkpoint*

---

### 11. 调研 (Fact Research)
为了核实客观事实（Ground Truth）而进行的查证与对比工作：深入项目代码库、配置与实际环境核实文档声称是否脱节，或对标成熟规范寻找遗漏分支。调研只提供事实与出入证据，不得把外部做法直接写成对象的完成条件。

*避免使用：研究、调查、research*

---

### 12. 核查 (Verification)
调研的一种形式：专门查证对象文档中所引用的命令、脚本、路径、接口字段、环境变量或外部组件在当前项目中是否真实有效且匹配。

*避免使用：验证、校验*

---

### 13. 对标 (Benchmarking)
调研的一种形式：查阅业界同类成熟规范、标准化 SOP 或项目最佳实践，用于对照寻找对象中遗漏的关键异常处理、防御性校验或退出分支。其产出本身不能直接当作真问题。

*避免使用：参考、竞品分析、benchmark*

---

### 14. 候选标准 (Candidate Criterion)
调研或陪审员提出的、对象自己没写但认为应当补上的新判据。必须单独编号列出并标明来源，不得直接当完成条件使用；只有在第 5 步闸门被人批准后才升格为本轮完成条件。这是防止“别人有我也要有”的产品缺口污染判据的关卡。

*避免使用：新增要求、补充条件、best practice*

---

### 15. 产品缺口 (Feature Gap)
同类实现具备、但本对象的完成条件并不要求的能力差异。第 4 步一律淘汰，不得升级成真问题。

*避免使用：缺失功能、待优化项*

---

### 16. 改法 (Proposed Fix)
针对某条获批真问题或某项用户指定改动给出的具体修改方案。真问题的改法要说明修改后如何让完成条件重新成立；用户指定改动的改法要给出影响面清单。

*避免使用：方案、修复、patch*

---

### 17. 影响面清单 (Blast Radius List)
一项用户指定改动必须同步修改的全部位置逐条列举，例如改名会波及文件名与目录名、文件内的标识字段、说明文档、安装或调用路径与其它文件里的交叉引用。第 8 步必须一次性原子改完，不允许只改一半。

*避免使用：波及范围、关联文件、checklist*

---

### 18. 异议 (Critique / Objection)
在交叉审查阶段对改法草案提出的反对意见（例如：与清单不符、破坏其它完成条件、遗漏获批条目、超出修改边界、影响面清单有遗漏）。提出异议的模型必须与写改法的模型不同。

*避免使用：挑刺、评审意见、review comment*

---

### 19. 笔记 (Execution Notes)
本次运行全过程的完整记录文档，包括两节清单、候选标准、淘汰原因、调研事实及终点去向表。统一保存在本仓库的 `docs/` 目录下。

*避免使用：日志、报告*
